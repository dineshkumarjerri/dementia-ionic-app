import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dementia-care-app-ionic',
  webDir: 'www'
};

export default config;
